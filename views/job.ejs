<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Details</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Job Details</h1>
        <div class="card">
            <div class="card-body">
                <h2 class="card-title"><%= job.title %></h2>
                <p class="card-text"><strong>Description:</strong> <%= job.description %></p>
                <p class="card-text"><strong>Location:</strong> <%= job.location %></p>
                <p class="card-text"><strong>Posted By:</strong> <%= job.postedBy.name %> (ID: <%= job.postedBy.college_Id %>)</p>
                <p class="card-text"><strong>Posted At:</strong> <%= job.postedAt.toDateString() %></p>
                <p class="card-text"><strong>Application Link:</strong> <%= job.applyLink ? job.applyLink    : 'N/A' %></p>

                <a href="/jobs/<%= job._id %>" class="btn btn-warning">Update</a>
                <form action="/jobs/<%= job._id %>" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
        <a href="/api/v1/jobs" class="btn btn-primary mt-3">Back to Jobs</a>
    </div>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
